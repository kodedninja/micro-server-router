{
  "name": "micro-server-router",
  "version": "1.0.1",
  "description": "Server router for Micro",
  "main": "index.js",
  "scripts": {
    "deps": "dependency-check . && dependency-check . --extra --no-dev",
    "test": "standard && npm run deps && NODE_ENV=test node test",
    "test:cov": "standard && npm run deps && NODE_ENV=test istanbul cover test.js"
  },
  "repository": "kodedninja/micro-server-router",
  "keywords": [
    "server",
    "router",
    "trie",
    "wayfarer",
    "micro"
  ],
  "license": "MIT",
  "dependencies": {
    "pathname-match": "^1.1.3",
    "urlencode": "^1.1.0",
    "wayfarer": "^6.1.4"
  },
  "devDependencies": {
    "concat-stream": "^1.5.1",
    "dependency-check": "^2.5.1",
    "get-server-port": "^1.0.0",
    "istanbul": "^0.4.1",
    "standard": "^8.0.0",
    "tape": "^4.2.2"
  }
}
